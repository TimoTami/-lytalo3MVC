@model IEnumerable<Älytalo3MVC.Models.Talo>

@{
    ViewBag.Title = "Index";
}

<h2>Talot</h2>

<p>
    @Html.ActionLink("Uusi talo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName( "Talon nimi")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TalonNimi)
        </td>
        <td>
            @Html.ActionLink("Talon tiedot", "Timo2", new { id = item.TaloID }) |
            @Html.ActionLink("Nimen muutos", "Edit", new { id = item.TaloID }) |
            @Html.ActionLink("Poista", "Delete", new { id = item.TaloID })

            @*<a class="btn btn-default" href="@Url.Action("Delete", new {id = item.TaloID })" role="button">
            <span class="glyphicon glyphicon-trash">Poista</span>
        </a>*@

        </td>
    </tr>
}

</table>
